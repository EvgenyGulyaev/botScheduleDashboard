<INSTRUCTIONS>
@/Users/evgeny/.codex/RTK.md

## Required Skills

- Use `superpowers:using-superpowers` at the start of each task to select the right workflow.
- Use `superpowers:test-driven-development` for new behavior, bug fixes, and refactors.
- Use `superpowers:systematic-debugging` before changing code in response to a failing test or unexpected behavior.
- Use `superpowers:verification-before-completion` before reporting work as complete or tests as passing.
- Use `code-review-excellence` when reviewing branch changes, pull requests, or implementation quality.

## Project Notes

- This is a Go backend project (`go.mod`) using `silverlining` and `bbolt`; do not add frontend Redux/Redux Toolkit dependencies here.
- Keep shell commands prefixed with `rtk` according to the imported RTK instruction.
</INSTRUCTIONS>
